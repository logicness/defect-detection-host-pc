# 2026-08-11 工作日志

## 项目分析（只读）
- 对 Host PC 项目做了全量分析：27 文件、4,313 行 Python + 174 行 QSS（共约 635KB）。
- 分层：main.py(533) → pages 5页(1,662) + components(440) → core 9 模块(1,308) → assets/tools/data。
- 验证：offscreen 冒烟测试 8/8 通过；data/defect_host.db 含 defect_records 1000 条 + production_stats，演示数据完整。
- 结论：v1.0 状态健康、可直接运行；待办仍为相机接入/模型管理/下位机联调/NPU 占用/自动化测试/PyInstaller 打包（见 AGENTS.md）。
- 输出了架构总览图（分层结构图）。

## v1.1 参考 Nano 版完善（17 处文件改动，冒烟 8/8 通过）
- UI 放大：QSS 全局 13→16px，按钮/输入框 min-height 36px，Tab 17px，表格 16px，KPI 44px，
  Toggle 48x26，状态栏 15px，标题栏 20px；页面内联 12-13px 统一 16px；
  common_widgets 补齐 SectionTitle/PageHeader/Feedback
- 新增：components/stats_charts.py（matplotlib 趋势+饼图，历史页 A4）、
  components/roi_editor.py（RoiCanvas 拖拽/缩放手柄 + RoiEditDialog 表单双向同步，像素坐标）、
  components/model_manager_dialog.py（Nano 清单+本地模型选择）、
  core/local_infer.py + core/local_postprocess.py（ONNX/PT 本地推理，流引擎新增 local 模式）
- database.py 新增 get_daily_stats/get_defect_type_stats；main 推理源优先级 tcp>local>sim；
  _on_load_model 识别本地 .onnx/.pt；页面加 set_cur_model
- 依赖追加 matplotlib/onnxruntime 到 requirements.txt
- 待办：真机模型验证（.onnx 放入后参数页/模型管理选择即启用 local）

## 本地图片检测功能（v1.1 补充）
- 新增 components/local_image_detect.py：LocalImageDetectDialog（选图 → 本地检测 →
  红框+ROI 叠加 → 保存标注图+CSV）；有本地模型走 LocalInferEngine 真实推理，
  无模型走 _sim_detect_on_image 模拟演示（随机 1-2 缺陷框），模型文件不存在自动回退模拟
- main.py 菜单升级为 QMenu（本地图片检测/模型管理/关于），结果经 result_committed
  信号回 controller.ingest_result 统一入管线（KPI/写库/历史）
- 入口：标题栏 ≡ 菜单 → 本地图片检测；冒烟 8/8 + 组件自测通过
